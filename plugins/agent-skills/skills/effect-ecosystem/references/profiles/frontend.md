# Profile: frontend

Load when `.effect-skill.json` contains `frontend` or browser UI packages.

Use `references/effect-atom.md` for state and `references/effect-form.md` for
forms. Rule facts remain in `contracts/rules.json`.
