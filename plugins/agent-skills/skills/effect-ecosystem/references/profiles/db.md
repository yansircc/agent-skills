# Profile: db

Load when `.effect-skill.json` contains any `db:*` shape.

Use `references/sql-and-rpc.md` for `@effect/sql-*` adapters, `SqlClient`,
migrations, transactions, and schema-bound persistence boundaries.
