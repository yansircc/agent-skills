# Profile: rpc

Load when `.effect-skill.json` contains `rpc`.

Use `references/sql-and-rpc.md` and `references/schema.md` for request,
response, and error schemas. Avoid hand-written transport glue.
