# Profile: http-client

Load when `.effect-skill.json` contains `http-client`.

Use `references/platform-http.md` and `references/schema.md` for HttpClient,
retry, tracing, and schema-backed decode boundaries.
