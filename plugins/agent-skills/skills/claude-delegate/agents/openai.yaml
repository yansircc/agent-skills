interface:
  display_name: "Claude Delegate"
  short_description: "Delegate bounded work to local Claude CLI"
  default_prompt: "Use $claude-delegate to run a bounded task through the local Claude CLI and return structured results."
