# Profile: node

Load when `.effect-skill.json` contains `node`.

Use `@effect/platform-node` services for filesystem, path, terminal runtime,
configuration, and process boundaries. Host tooling in this repository is
declared separately from target application code.
