# Profile: http-server

Load when `.effect-skill.json` contains `http-server`.

Use `references/platform-http.md`, `references/schema.md`, and
`references/observability.md` for HttpApi, boundary schemas, and top-level
runtime wiring.
