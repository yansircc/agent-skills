# Effect v3 Notes

Use local `node_modules/effect` as API truth. Prefer `Effect.gen(function* () {
... })`, `yield*`, `Data.TaggedError`, `Layer.Layer<ROut, E, RIn>`, and
ecosystem packages that match the installed v3 workspace.
